name: Build Windows Installer

on:
  push:
    branches:
      - main
      - feature/minno-desktop-download
    paths-ignore:
      - "android/**"
      - ".github/workflows/android-build.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Python build dependencies
        shell: pwsh
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Run desktop unit tests
        shell: pwsh
        run: python -m unittest discover -s tests -v

      - name: Syntax check
        shell: pwsh
        run: |
          python -m py_compile main.py desktop_app.py
          python -m compileall -q sources
          python -c "import desktop_app; print('desktop import OK')"

      - name: Generate application icon
        shell: pwsh
        run: python assets\generate_icons.py --platform windows

      - name: Download yt-dlp, Deno and FFmpeg
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: .\prepare_tools.ps1

      - name: Build Windows application
        shell: pwsh
        run: |
          python -m PyInstaller --noconfirm --clean --windowed --onedir --name KidsChurchVideoDownloader --icon assets\generated\youtube_downloader.ico desktop_app.py
          Copy-Item tools\yt-dlp.exe dist\KidsChurchVideoDownloader\yt-dlp.exe
          Copy-Item tools\deno.exe dist\KidsChurchVideoDownloader\deno.exe
          Copy-Item tools\ffmpeg.exe dist\KidsChurchVideoDownloader\ffmpeg.exe
          Copy-Item tools\ffprobe.exe dist\KidsChurchVideoDownloader\ffprobe.exe
          Copy-Item THIRD_PARTY_NOTICES.md dist\KidsChurchVideoDownloader\THIRD_PARTY_NOTICES.md

      - name: Verify portable build contents
        shell: pwsh
        run: |
          $required = @(
            "KidsChurchVideoDownloader.exe",
            "yt-dlp.exe",
            "deno.exe",
            "ffmpeg.exe",
            "ffprobe.exe",
            "THIRD_PARTY_NOTICES.md"
          )
          foreach ($file in $required) {
            $path = Join-Path "dist\KidsChurchVideoDownloader" $file
            if (-not (Test-Path $path)) {
              throw "Missing packaged file: $path"
            }
          }

      - name: Install Inno Setup
        shell: pwsh
        run: choco install innosetup -y --no-progress

      - name: Build installer
        shell: pwsh
        run: |
          $pf86 = [Environment]::GetFolderPath("ProgramFilesX86")
          $iscc = Join-Path $pf86 "Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) {
            $iscc = Join-Path $env:ProgramFiles "Inno Setup 6\ISCC.exe"
          }
          if (-not (Test-Path $iscc)) {
            throw "Inno Setup compiler was not found."
          }
          & $iscc installer.iss

      - name: Upload installer
        uses: actions/upload-artifact@v4
        with:
          name: KidsChurchVideoDownloader-Windows-Installer
          path: installer_output\KidsChurchVideoDownloader_Setup_v0.7.0.exe
          if-no-files-found: error
          compression-level: 0

      - name: Upload portable build
        uses: actions/upload-artifact@v4
        with:
          name: KidsChurchVideoDownloader-Windows-Portable
          path: dist\KidsChurchVideoDownloader\
          if-no-files-found: error
          compression-level: 0
